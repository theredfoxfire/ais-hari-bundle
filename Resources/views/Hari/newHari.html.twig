{% block content %}
    <h1>Create Hari Form</h1>
    {%  if (form is not null) %}
    <form action="{{ url('api_1_post_hari') }}" method="POST" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" value="submit">
    </form>
    {% endif %}
{% endblock %}
